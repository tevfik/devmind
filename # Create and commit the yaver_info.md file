echo 'Yaver is an autonomous agent' > yaver_info.md
git add yaver_info.md
git commit -m "Add yaver_info.md with content 'Yaver is an autonomous agent'"
git push origin main  # Assuming the default branch name is 'main'
